// src/api/user.ts
import { api } from '.';

// export async function getUserInfo(username: string) {
//   const { data } = await api.get(`/user/getUserInfo/${username}`);
//   return data as User;                     // <-- define User interface
// }

// export async function updateUser(
//   username: string,
//   first: string,
//   last: string,
//   dobISO: string,
// ) {
//   await api.patch(`/user/updateUser/${username}`, {
//     first_name: first,
//     last_name:  last,
//     DOB:        dobISO,
//   });
// }
